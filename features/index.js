'use strict';

/**
 * @file index.js
 * It controls the flow of features
 */

const FileJSON = require('./FileJSON')
const FileManager = require('./FileManager')

module.exports = {
    FileJSON,
    FileManager
}