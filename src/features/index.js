'use strict'

/*
:--------------------------------------------------------------------------
: [features]
:--------------------------------------------------------------------------
:
:   This content shows up the features for each Instance.
*/

const FileJSON = require('./json')
const FileManager = require('./file')

module.exports = {
    FileJSON,
    FileManager,
}
