export type FS_CONSTANTS_ACCESS = 'F_OK' | 'R_OK' | 'W_OK';

export type FileManagerDataContext = {
    routeName: string;
    filename: string;
    field: string;
};

export interface PathFileManagerStructure {}
